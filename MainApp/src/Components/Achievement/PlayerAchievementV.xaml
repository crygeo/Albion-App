<UserControl x:Class="Albion_App.Components.Achievement.PlayerAchievementV"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Albion_App.Components.Achievement"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance local:AchievementVm}">


    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border Width="64" Height="64" CornerRadius="50">
            <Grid Background="Transparent">

                <!-- Loading -->
                <ProgressBar
                    IsIndeterminate="True"
                    Width="20"
                    Height="20"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">

                    <ProgressBar.Style>
                        <Style TargetType="ProgressBar" BasedOn="{StaticResource MaterialDesignCircularProgressBar}">

                            <Setter Property="Visibility"
                                    Value="Collapsed" />

                            <Style.Triggers>

                                <!-- Mostrar cuando no hay imagen -->
                                <DataTrigger Binding="{Binding Image}"
                                             Value="{x:Null}">

                                    <Setter Property="Visibility"
                                            Value="Visible" />

                                </DataTrigger>

                            </Style.Triggers>

                        </Style>
                    </ProgressBar.Style>

                </ProgressBar>


                <!-- Imagen -->
                <Image Source="{Binding Image}"
                       Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="HighQuality">

                    <Image.Style>
                        <Style TargetType="Image">

                            <Setter Property="Visibility"
                                    Value="Visible" />

                            <Style.Triggers>

                                <DataTrigger Binding="{Binding Image}"
                                             Value="{x:Null}">

                                    <Setter Property="Visibility"
                                            Value="Collapsed" />

                                </DataTrigger>

                            </Style.Triggers>

                        </Style>
                    </Image.Style>

                </Image>

            </Grid>
        </Border>
        <TextBlock Grid.Row="1" Text="{Binding Level}"
                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
        

        <Grid.ToolTip>
            <ToolTip>
                <StackPanel>
                    <TextBlock FontWeight="Medium" Text="{Binding DisplayName}"
                               FontSize="13" />

                </StackPanel>
            </ToolTip>
        </Grid.ToolTip>
    </Grid>
</UserControl>